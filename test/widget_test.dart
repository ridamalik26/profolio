import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // Auth tests require Firebase — use integration_test package for those.
    expect(true, isTrue);
  });
}
